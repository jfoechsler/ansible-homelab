all:
  children:
    vm_hosts:
      hosts:
        myserver:
          ansible_host: localhost

    routers:
      children:
        vm_routers:

    # Runtime groups used by vm-inventory
    staging_vms:
    vms:
