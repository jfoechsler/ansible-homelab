
- set_fact:
    vmfs_current: "{{ vmfs_current + [{'vm': vm.value.name, 'source': item.source['@dir'], 'target': item.target['@dir']}] }}"
  with_items:
    - "{{ vars['dom_dict_' + vm.value.name].domain.devices.filesystem }}"
  when: item.source['@dir'] is defined
